import React from "react";
import "./Cart.css";

const Cart = (props) => {
  return (
    <button type="button" className="cart-button large-sc-cart">
      سبد خرید
    </button>
  );
};

export default Cart;
